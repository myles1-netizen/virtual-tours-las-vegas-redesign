---
export interface Props {
  eyebrow?: string;
  title: string;
  lead?: string;
  align?: "left" | "center";
  as?: "h1" | "h2";
  class?: string;
  /** CMS: prefix for data-cms attributes, e.g. "why" → data-cms="why.title" */
  cmsPrefix?: string;
}
const {
  eyebrow,
  title,
  lead,
  align = "left",
  as = "h2",
  class: className = "",
  cmsPrefix = "",
} = Astro.props;
const Tag = as;
const cms = (field) => (cmsPrefix ? { "data-cms": `${cmsPrefix}.${field}` } : {});
---
<div class:list={["section-head", `align-${align}`, className]} data-reveal>
  {eyebrow && <p class="eyebrow" {...cms("eyebrow")}>{eyebrow}</p>}
  <Tag class="display title" {...cms("title")}>{title}</Tag>
  {lead && <p class="lead" set:html={lead} {...cms("lead")} />}
</div>

<style>
  .section-head { max-width: 70ch; }
  .align-center { margin-inline: auto; text-align: center; display: flex; flex-direction: column; align-items: center; }
  .align-center .lead { margin-inline: auto; }
  .title {
    font-size: var(--step-4);
    line-height: var(--leading-tight);
  }
  .align-center .title { max-width: 22ch; }
  .lead { margin-top: 1.1rem; }
</style>
